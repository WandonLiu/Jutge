#include <iostream>
using namespace std;

int main(){
    int pos, num, i = 1;
    cin >> pos;
    while (cin >> num and i < pos){
        ++i;
    }
    cout << "At the position "<< pos << " there is a(n) " << num << '.' << endl;
}